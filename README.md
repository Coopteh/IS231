## Методы модульного тестирования по белому ящику

Предварительная работа с git
```
запустите Git Bash
перейдите в каталог c:/xampp/htdocs
> cd c:/xampp/htdocs

Переключитесь на ветку main
Создайте новую ветку unit-test-white
Создайте файл .gitignore (vendor)
```
Откройте на редактирование файл README.md

Дайте определение - тестированию по белому ящику.     

Опишите методы тестирования по белому ящику:  
- Метод покрытия операторов.   
- Метод покрытия условий.  
- Метод комбинаторного покрытия условий.  

Опишите каждый метод в файле.

Создайте файл ./src/DiscountService.php
```
<?php
namespace App;

class DiscountService
{
    /**
     * Рассчитывает процент скидки.
     *
     * @param int $age Возраст клиента
     * @param bool $isVip Статус VIP клиента
     * @param float $amount Сумма заказа
     * @return float Процент скидки (0.0 - 0.5)
     * @throws \InvalidArgumentException Если сумма заказа некорректна
     */
    public function calculate(int $age, bool $isVip, float $amount): float
    {
        // Проверка на валидность суммы (Ветка 1)
        if ($amount <= 0) {
            throw new \InvalidArgumentException("Сумма заказа должна быть больше нуля");
        }

        $discount = 0.0;

        // Сложное условие (Ветка 2)
        if ($amount > 1000 && $isVip) {
            $discount = 0.20; // 20%
        } 
        // Вложенное условие (Ветка 3)
        elseif ($amount > 500) {
            if ($age > 60 || $isVip) {
                $discount = 0.10; // 10%
            }
        }

        return $discount;
    }
}
```
Основное задание:  
```
составьте 3 файла с тестами для phpUnit в папке ./tests
- 1 файл содержит тесты проверки метода покрытия операторов  
- 2 файл содержит тесты проверки метода покрытия условий  
- 3 файл содержит тесты проверки метода комбинаторного покрытия условий
```
Добейтесь успешного выполнения всех тестов

Работа с git
```
Закоммитьте и запуште изменения.
Создайте Pull request.
```
